 services:
  - type: web
    name: fastapi-insurance-app
    env: python
    region: oregon
    plan: free
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn insurance_app_main:app --host 0.0.0.0 --port 10000"
    workingDir: zorma
    pythonVersion: 3.11